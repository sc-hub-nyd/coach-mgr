# CoachMgr インラインスタイル移行台帳

**基準日：2026-08-18**
**対象リリース：v1.27.0**
**対象：`index.html`、動的HTMLを生成する`*.js`モジュール**

## 1. 目的

インラインスタイルは、すべて削除すること自体を目的にしません。画面構造・余白・色・形状の反復指定はトークン、`l-*`、`c-*`へ移し、JavaScriptが実行時に計算する座標、寸法、表示状態だけを理由付きで残します。

`index.html`には469件の`style`属性が残っています。これらは、一括した正規表現置換を行わず、画面・パターン単位で移行します。

## 2. 分類規則

| 区分 | 定義 | 移行先または扱い |
|---|---|---|
| `共通化` | 余白、幅、整列、色、角丸、フォント、表示の反復指定。 | トークン、`l-*`、`c-*`、修飾子。 |
| `画面固有` | 画面内で意味が明確な一回限りの構造指定。 | 画面固有クラス。IDへの視覚指定はしない。 |
| `動的維持` | JSが計算・更新する位置、幅、高さ、transform、canvas座標。 | 必要最小限のstyleを残し、生成関数に理由をコメントする。 |
| `要調査` | 条件分岐、旧互換、外部ライブラリ依存で影響範囲が未確定。 | 実行時表示・イベント接続を確認してから分類する。 |

## 3. 優先移行対象

| 優先度 | 対象画面・モジュール | 主なインライン指定 | 区分 | 置換先 | 移行波 |
|---:|---|---|---|---|---:|
| 1 | ダッシュボード / `app.js` | KPI、ランキング、予定、空状態、役割別導線の余白・表示。 | 共通化 / 要調査 | `c-card`、`c-data-list`、`c-empty-state`、`c-liquid-panel`。 | 1 |
| 1 | 選手 / `players.js` | 選手カード、比較指標、参加履歴、編集導線。 | 共通化 | `c-data-list`、`c-card`、`c-modal`。 | 1〜2 |
| 1 | 振り返り / `insights.js` | KPI、判断カード、履歴、空状態。 | 共通化 | `c-data-list`、`c-card`、`c-empty-state`。 | 1 |
| 2 | メニュー・戦術 / `library.js`、`tactics.js` | 作成／編集フォーム、タグ、一覧補助表示。 | 共通化 / 画面固有 | `c-modal`、`c-form-field`、`c-card`。 | 2 |
| 2 | 試合詳細 / `matches.js` | 招集、ログ、スコア、編集フォーム。 | 共通化 / 動的維持 | `c-roster-row`、`c-data-list`、`c-action-group`。 | 3 |
| 3 | Field Companion | 片手操作面、時計、スコア、イベント登録位置。 | 画面固有 / 動的維持 | 意味トークン、`c-icon`、`c-roster-row`。 | 4 |
| 3 | 作図・アニメーション / `drawing.js` | キャンバス寸法、座標、ツール状態、フレーム位置。 | 動的維持 / 共通化 | 通常UIのみ`c-action-group`、`c-form-field`、`c-modal`。 | 5 |

## 4. 置換パターン

| 既存の指定例 | 移行先 | 注意点 |
|---|---|---|
| `style="margin-top: ..."`、`gap`、`display:flex` | `l-stack`、`l-cluster`、`c-action-group` | 画面内の一回限りの座標指定と混同しない。 |
| `style="padding: ..."`、`border-radius: ..."` | `--surface-inset-*`、`--control-inset-*`、`--control-radius` | 文字開始位置と角からの距離を共通化する。 |
| `style="background: ..."`、`color: ..."` | `--color-*`、`--neo-*`、`--glass-*`、状態修飾子 | 種色・固定白黒を直接指定しない。 |
| `style="width: ..."`、`height: ..."` | `l-grid`、コンテナクエリ、画面固有クラス | canvas、動的プログレス、計測値は維持できる。 |
| `style="display:none"` | `hidden`、`is-*`、roleクラス | JSが直接切り替える既存契約を壊さない。 |
| `style="transform: ..."` | 専用クラスまたは動的維持 | 作図・アニメーションの座標計算は一般化しない。 |

## 5. 禁止事項

- `style`属性を削除するためだけに、意味のない単発クラスを増やさない。
- IDを新しい視覚スタイルのフックにしない。
- JSが設定する座標・寸法をCSSへ移し、描画・試合中表示を壊さない。
- インライン指定の削除と、データ保存・同期・イベント仕様の変更を同じコミットに混在させない。
- 動的維持と判断した指定を、理由なしに放置しない。

## 6. 画面別完了条件

| 画面 | インライン指定の移行完了条件 |
|---|---|
| ダッシュボード | カード、KPI、ランキング、空状態、操作群に関する反復指定が共通部品へ移る。role別分岐とイベントIDは維持する。 |
| 選手 | 比較・履歴・編集導線の反復指定が一覧・モーダル部品へ移る。CSVと詳細遷移は維持する。 |
| 振り返り | KPI、判断カード、履歴、空状態の余白・色・配置が共通化される。根拠と推奨の意味は維持する。 |
| 試合詳細 | 招集・ログ・スコア周辺の通常UIを共通化し、試合イベント・集計・保存値は維持する。 |
| Field Companion | 操作安全性・状態可視性を優先し、動的操作面に必要な指定は理由付きで残す。 |
| 作図・アニメーション | ツールバー、設定、確認UIは共通化し、キャンバス・ドラッグ・フレーム座標は維持する。 |

## 7. 台帳更新手順

1. 画面移行の開始時に、対象指定を`進行中`としてコミット計画へ記録する。
2. 置換後、削除した指定数ではなく、移行した**パターン**、置換先、維持理由を記録する。
3. P33、P34、P35、対象機能スモーク、レスポンシブ25件を通過した時点で`完了`へ変更する。
4. 残した動的指定は、対象画面・理由・再評価する波を記載する。
