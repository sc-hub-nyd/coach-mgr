# CoachMgr デザインシステム変更履歴

この文書は、アプリ機能の更新履歴とは別に、再利用可能なデザインシステム契約の追加・変更・非推奨化を追跡する。画面固有の見た目だけを変えた変更は記録しない。トークン、共通部品、アイコン体系、アクセシビリティ、品質ゲート、PWA資産へ影響する変更を記録する。

## v1.31.17 — 2026-08-21

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| アイコン体系 | アプリ実行時のカスタムSVG、CSSマスク、`c-icon--*`、専用precacheを廃止し、ローカルTabler Iconsのみへ統一した。 | `currentColor`によるテーマ追従、`c-tabler-icon--*`の意味トーン、ローカルCSS・WOFF2、外部CDN非依存を維持する。 | P33、P42、P55、P56 |
| モバイルナビ | コーチ用ボトムナビをホーム → 選手管理 → 試合／練習 → メニュー／戦術 → その他の5項目に再構成した。 | 保護者モードの既存ホーム・試合・練習・その他の導線、役割権限、既存ルート名を維持する。 | P54、P56、手動確認 |
| 二択シート | 試合／練習とメニュー／戦術を、説明付き選択肢を持つ共通bottom-sheetへ変更した。 | `role="dialog"`、見出し・説明、フォーカストラップ、トリガーへのフォーカス復帰、Safe Area、動きを減らす設定を共通モーダル規則で維持する。 | P49、P54、P56 |
| PWA・統制 | cache世代を`coachmgr-v244`へ更新し、削除済みSVG参照をprecacheから除去した。 | 更新通知を通じて既存端末へHTML・CSS・JavaScript・Tablerサブセットを確実に配信する。 | P47、P49、P55、全契約テスト |

## v1.31.16 — 2026-08-21

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| チーム識別子 | ソース、設定、テスト、資料、スライドから特定チーム名・略称・外部ブランド参照を除去し、汎用的なチーム表現へ統一した。 | 既存チームデータ、任意のチームカラー、保存済み種色、同期形式は変更しない。Git履歴は書き換えない。 | P55、横断監査 |
| カスタムSVG | `assets/icons/team/`へ汎用チームアイコンを再配置し、固有名称を含むカスタムSVGファイル名を意味名へ変更した。 | `c-icon--*`の意味クラス、CSSマスク、`currentColor`、Tabler併用、44個の資産数を維持する。 | P33、P42、P55 |
| PWA | Service Workerのprecacheを新しい汎用アイコンパスへ更新し、cache世代を`coachmgr-v243`へ更新した。 | 既存PWA端末には更新通知を通じて新しいHTML・CSS・SVG参照を配信する。 | P47、P49 |
| 再発防止 | P55を追加し、特定チーム名・略称を含むパスと、ソース・設定・ドキュメント・SVGのテキスト再導入を検出する。 | 生成済みブラウザ検証アーティファクトとGitメタデータは検査対象外とする。 | P55、全契約テスト |

## v1.31.15 — 2026-08-21

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| 役割切替 | 保護者／コーチの両方を、保護者アイコン＋保護者、コーチアイコン＋コーチとして常時表示する二択セグメントへ変更した。 | 既存の役割切替ID、パスコード確認、即時保護者切替、権限制御、`aria-pressed`と状態別`aria-label`を維持する。 | P38、手動確認 |
| 表示テーマ切替 | Light／Darkの両方を、太陽アイコン＋ライト、月アイコン＋ダークとして常時表示する二択セグメントへ変更した。 | 既存のテーマ切替ID、`role="switch"`、`aria-checked`、状態別ラベル、テーマ適用、色モード変更通知を維持する。 | P47、P34、手動確認 |
| 選択状態 | 円形スライダーを廃止し、選択中のセグメントだけをsurface・brand文字色・shadowで強調する。 | LightのLiquid Glass、Darkの深いチャコール、チームカラー由来のセマンティックトークン、フォーカスリングを維持する。 | P38、P47、P53 |
| モーション | 選択面の色・shadow変化のみを共通トークンで扱い、動きを減らす設定では1msへ縮退する。 | 役割・テーマの二択部品で同一の形状・状態・縮退モーション規則を使う。 | P38、P47 |
| PWA・統制 | cache世代を`coachmgr-v242`へ更新し、役割・テーマ双方のアイコン付きテキスト構造と選択状態を契約化した。 | 更新通知を通じて既存端末へHTML・CSS・JavaScriptを確実に配信する。 | P38、P47、P49、全契約テスト |

## v1.31.14 — 2026-08-21

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| 役割モード切替 | PCサイドバーとモバイルその他メニューの保護者／コーチ切替を、ラベル・円形スライダーを持つ二択ピル型`c-role-mode-switch`へ統一した。 | 既存の`btn-toggle-role`／`mobile-btn-toggle-role` ID、コーチパスコード確認、保護者への即時切替、権限制御を維持する。 | P38、手動確認 |
| 状態・アクセシビリティ | 保護者は左、コーチは右へスライダーを移動する。役割はON／OFFではないため`role="switch"`を使わず、`aria-pressed`、状態別`aria-label`、`data-user-role`で現在状態を同期する。 | Enter／Spaceのbutton操作、フォーカスリング、テーマトークン、LightのLiquid Glass、Darkの深いチャコールを維持する。 | P38、P47 |
| モーション | スライダーは`duration-base`と`ease-spring`で収束し、動きを減らす設定では1msへ縮退する。 | テーマ切替のピル部品と形状・surface・shadow・縮退モーションの設計言語を共有する。 | P38、P47 |
| PWA・統制 | cache世代を`coachmgr-v241`へ更新し、役割ピルの構造・コーチ側スライダー位置・ARIA同期・縮退モーションをP38で契約化した。 | 更新通知を通じて既存端末へHTML・CSS・JavaScriptを確実に配信する。 | P38、P47、P49、全契約テスト |

## v1.31.13 — 2026-08-21

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| サイドバーユーティリティ | PC左サイドメニュー下部を、モード → クラウド同期 → 表示テーマ → バージョンの縦並びへ再構成した。 | 既存の役割バッジ、モード切替、同期ポップオーバー、テーマ切替、更新履歴の各ID・イベント・ARIAを維持する。 | P38、P47、手動確認 |
| 行レイアウト | モード行と表示テーマ行を同じ全幅・最小行高・左右整列の共通レイアウトへそろえ、同期行も同じ最小操作高へ統一した。 | 既存の`c-theme-mode-switch`、セマンティックトークン、LightのLiquid Glass、Darkの深いチャコールを維持する。 | P38、P53 |
| PWA・統制 | cache世代を`coachmgr-v240`へ更新し、PCサイドバーユーティリティのDOM順と行高整合性をP38で契約化した。 | 更新通知を通じて既存端末へHTML・CSS・バージョン情報を確実に配信する。 | P38、P47、P49、全契約テスト |

## v1.31.12 — 2026-08-21

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| テーマ切替UI | PCサイドバーとモバイルその他メニューの色モード操作を、太陽・月・円形スライダーを持つピル型`c-theme-mode-switch`へ統一した。 | 既存の切替ID・保存設定・テーマ適用・色モード変更通知を維持し、表示部品のみを置き換える。 | P34、P47、手動確認 |
| 光・モーション | 参照演出の即時テーマ反映を維持しつつ、スライダーだけを`duration-base`と`ease-spring`で収束させる。Lightは太陽側、Darkは月側へ移動する。 | ダークテーマは純黒ではなく既存の深いチャコールを維持し、Lightは既存のLiquid Glass表現を維持する。 | P47、P34 |
| アクセシビリティ | 各切替を`role="switch"`、`aria-checked`、状態別`aria-label`、focus-visibleへ対応させ、動きを減らす設定では1msへ縮退させる。 | Enter／Spaceによるbutton操作、既存の日本語トースト、PC／モバイルの同時状態同期を維持する。 | P47、P38 |
| PWA・統制 | cache世代を`coachmgr-v239`へ更新し、ピル構造・ダーク時のスライダー位置・状態同期・動きを減らす設定をP47で契約化した。 | 更新通知を通じて既存端末へCSS・JS・HTMLを確実に配信する。 | P47、P49、全契約テスト |

## v1.31.11 — 2026-08-21

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| PWAアイコン | 既存ネオンアイコンから`COACHMGR`文字を外し、盾とボールを中央の安全領域へ光学的に再配置した。 | OSが表示するアプリ名との二重表記を解消し、赤・コーラル・エメラルド・深いチャコールのブランド世界観を維持する。 | P47、PNG寸法確認、実機確認 |
| 用途別アセット | 通常PWA（192px／512px）、maskable（512px）、Apple Touch Icon（180px）、Favicon（16px／32px）を専用PNGへ分離した。 | `manifest.json`、HTMLのApple／Favicon参照、Service Worker precacheを同一セットへ統一し、旧SVG・JPEGを参照しない。 | P47、P49、Service Worker確認 |
| PWA・統制 | cache世代を`coachmgr-v238`へ更新し、manifestの通常・maskable宣言とHTML・precacheの参照をP47で契約化した。 | 更新通知を通じて既存端末へ新しいアイコン資産を確実に配信する。 | P47、P49、全契約テスト |

## v1.31.10 — 2026-08-21

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| モバイル作図の順序 | `anim-canvas-area`を幅768px以下で`display: contents`へ切り替え、内部のピッチ・タブ・シーン列・プレイヤー操作をワークスペースの同一flex列で制御する。 | デスクトップ時のCanvas領域、絶対配置のプレイヤー操作、既存DOM ID・イベント・Canvasレイヤーを変更しない。 | P40、P37、実機確認 |
| 描画モード | `canvas-wrapper`をorder 1、モードタブをorder 2、ツールドックをorder 3、下部詳細をorder 10へ固定した。 | 描画タブではピッチ→タブ→ツールドック→メニュー詳細の順を保ち、ツールと詳細の間の不要な空白・分断をなくす。 | P40、モバイル実機確認 |
| アニメーションモード | 描画ツールドックを非表示とし、タブ後にシーン列・プレイヤー操作・下部詳細を並べる既存規則を同一flex列で有効化する。 | シーン操作、動画出力、フレーム選択、下部のシーン詳細フォームを維持する。 | P40、P37、P47 |
| PWA・統制 | cache世代を`coachmgr-v237`へ更新し、モバイル作図の順序契約をP40へ追加した。 | 更新通知を通じて既存端末へCSS変更を確実に配信する。 | P40、P47、P49、全契約テスト |

## v1.31.3 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| Canvasテーマ同期 | `library.js`に、表示済みの`library-mini-pitch-*` Canvasを保存済みframe・pitch templateで再描画する共通処理を追加した。 | 作図データ、テンプレート、Observerによるアニメーションプレビュー、メニュー管理のDOM ID・経路を変更しない。 | P34、P37、P40、実機確認 |
| テーマ切替通知 | アプリ内light/dark切替後、`applyCurrentTeamTheme`の完了後に`coachmgr:color-mode-changed`を発火する。ライブラリは次の描画フレームでCSS変数を読む。 | 通知は既存のボタン表示更新と同じテーマ状態を参照し、settings画面が発火する既存通知とも互換である。 | P34、テーマ切替確認 |
| 描画パレット | Canvasパレットは描画時にCSS custom propertyを解決するため、再描画後はlight/darkのpitch surface・line・guideを反映する。 | Canvas literal fallbackの所有者は`canvas-palette.js`に維持し、画面・部品層へ直接指定色を追加しない。 | P34、P51、P53 |
| PWA・統制 | cache世代を`coachmgr-v213`へ更新し、テーマ切替後の再描画通知を契約化した。 | 更新通知を通じて既存端末へJavaScript修正を配信する。 | P34、P47、P49、全契約テスト |

## v1.31.2 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| ライトの超高透過surface | 浮遊ナビのlight surfaceを26%へ下げ、白い反射を20%へ抑えた。brand 14% / companion 12%の低彩度反射を加え、ナビ内から背景の色差が透けて見えるようにした。 | 白基調のコンテンツ面・文字・カード、枠線なし、二層shadow、Liquid Lens選択面、ブランドラインを維持する。 | P47、light実機確認 |
| ライトの背景ベール | 画面下部radial veilを86%×31%、brand 24% / companion 14%へ強めた。 | チームカラー由来の淡い背景差だけを広げ、画面全体を強く着色しない。 | P34、P47、テーマ確認 |
| 屈折感 | `nav-floating-blur`を2.1rem、saturate 220%、brightness 1.05へ更新した。 | ダークモードのチャコールsurface、blur、面階層は変更しない。 | P47、dark回帰確認 |
| PWA・統制 | cache世代を`coachmgr-v212`へ更新した。 | 更新通知を通じて既存端末へ高透過CSS資産を配信する。 | P47、P49、全契約テスト |

## v1.31.1 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| ライトの高透過surface | 浮遊ナビのlight surfaceを40%まで高透過化し、白い反射を34%へ抑え、brand / companionの低彩度反射を少し強めた。 | 白基調のコンテンツ面、文字色、Liquid Lens選択面、ブランドライン、枠線なし・二層shadowを維持する。 | P47、light実機確認 |
| ライトの背景ベール | 画面下部radial veilを82%×27%へ拡大し、brand 18% / companion 10%の淡い色差を追加した。 | チームカラー変更時も意味トークンから導出し、画面全体を強く染めずナビ背後のガラス感だけを補助する。 | P34、P47、テーマ確認 |
| 屈折感 | `nav-floating-blur`を1.9rem、saturate 205%、brightness 1.04へ更新した。 | ダークモードのチャコールsurface・blurは変更しない。 | P47、dark回帰確認 |
| PWA・統制 | cache世代を`coachmgr-v211`へ更新し、ライトの高透過blurを契約化した。 | 更新通知を通じて既存端末へ新しいCSS資産を配信する。 | P47、P49、全契約テスト |

## v1.31.0 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| ライトの背景ベール | `liquid-veil`を、画面下部の淡いteam color radial veilと全体の低彩度gradientからなる二層背景へ変更した。白基調を維持しつつ、浮遊ナビ背後にだけ素材差を作る。 | コンテンツ面、文字、カード背景は白基調のまま維持し、背景画像を持つbody層だけを変更する。 | P47、light実機確認 |
| ライトの浮遊ガラス | nav surfaceをlight 54%へ高透過化し、弱いbrand/companion反射を加えた。`nav-floating-blur`を1.7rem、saturate 188%、brightness 1.03へ調整した。 | 枠線なし、二層shadow、選択面・ブランドライン、4/5タブ、Safe Area、動きを減らす設定を維持する。 | P47、P38、テーマ確認 |
| ダーク互換 | ダークテーマの`liquid-veil: none`、チャコール面階層、暗いnav surfaceは変更しない。 | v1.30.99で確定したダークの見た目を保持する。 | P34、dark実機確認 |
| PWA・統制 | cache世代を`coachmgr-v210`へ更新し、ライトの背景ベールと専用blurを契約化した。 | 更新通知を通じて既存端末へ新しいCSS資産を配信する。 | P47、P49、全契約テスト |

## v1.30.99 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| ライトのガラス表現 | 浮遊ナビのsurfaceをlight 64%へ高透過化し、ハイライトgradient、`nav-floating-blur`（1.55rem / saturate 175%）を追加した。 | 枠線なし、二層shadow、Liquid Lens選択面、4/5タブ、チームカラー追随を維持する。 | P47、light実機確認 |
| ダークの面階層 | 動的テーマのdark canvasを純黒から`#17191e`へ移行し、surface / raised / subtleを深いニュートラルチャコールとして再定義した。 | 本文・操作・状態色のコントラスト契約を維持し、チームカラーseedに関わらず同一のニュートラル階層を使う。 | P34（15 seed × 2 mode）、テーマ確認 |
| ダークの浮遊ナビ | ダーク時はチャコールsurfaceを82%で保持し、控えめなハイライトgradientとbrightnessを持つblurで存在感を確保する。 | 選択中のブランドライン・選択面、動きを減らす設定、Safe Area、その他シートとの積層を維持する。 | P47、P38、dark実機確認 |
| PWA・統制 | cache世代を`coachmgr-v209`へ更新し、テーマ別ガラスsurface・blurと純黒禁止を契約化した。 | 更新通知を通じて既存端末へCSSとテーマ生成ロジックを配信する。 | P34、P47、P49、全契約テスト |

## v1.30.98 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| 浮遊ナビの素材感 | 浮遊ボトムナビの枠線を除去し、`color-nav-floating-surface`をlight 72% / dark 68%へ強透過化した。`shadow-nav-floating`は下方向の二層shadowへ変更し、境界線に頼らず浮遊感を伝える。 | Liquid Lensの選択面、アイコン・ラベル、4/5タブ、チームカラー、light/darkの意味役割は維持する。 | P47、P34、テーマ確認 |
| その他メニュー | `c-mobile-more__sheet`をSafe Areaと`bottom-nav-float-gap`の上へ移動し、左右余白・全周角丸を持つ独立したシート面へ変更した。内部本文のSafe Area下余白も除去した。 | `modal-mobile-more`のARIA、Escape、Tab循環、フォーカス復帰、ロール・同期・経路操作、開閉時間を維持する。 | P49、P54、実機確認 |
| PWA・統制 | cache世代を`coachmgr-v208`へ更新し、境界線なしの浮遊ドックとその他シートの位置・形状を契約化した。 | 更新通知を通じて既存端末へCSS資産を配信する。 | P47、P49、P54、全契約テスト |

## v1.30.97 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| 浮遊ナビドック | `bottom-nav-height`、`bottom-nav-float-gap`、`bottom-nav-stack-gap`、`bottom-nav-reserve`を定義し、ナビをSafe Areaの上へ移動した。左右の余白、全周角丸、44px以上の操作領域を維持する。 | 既存の4/5タブ、`bottom-nav` ID、経路、`その他`の展開状態、詳細画面の戻る導線を変更しない。 | P38、P47、レスポンシブ確認 |
| フロスト透過面 | `color-nav-floating-surface`、`color-nav-floating-border`、`shadow-nav-floating`を追加し、ライト／ダーク・チームカラーへ追随する控えめな透過ドックを構成した。 | Liquid Lensの選択面とブランドラインはナビ項目内で継続し、背景面だけをフロスト透過へ置き換える。 | P47、P34、テーマ検証 |
| 積層・通知 | コンテキストバーを浮遊ナビの上へ再計算し、モバイルPWA更新通知を`bottom-nav-reserve`の上へ移動した。 | 詳細画面の戻る操作、PWA更新・閉じる操作、Safe Areaを維持し、操作面の重なりを避ける。 | P38、P47、公開PWA確認 |
| PWA・統制 | cache世代を`coachmgr-v207`へ更新し、浮遊位置、透過surface、コンテキストバー積層、更新通知位置を契約化した。 | 更新通知を通じて既存端末へ新しいナビ資産を配信する。 | P38、P47、P49、全契約テスト |

## v1.30.96 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| モーション体系 | `duration-micro`（80ms）、既存の`duration-fast`（140ms）・`duration-base`（200ms）、`duration-sheet`（260ms）・`duration-sheet-close`（180ms）を意味トークンとして定義した。 | 既存のtransitionは時間・イージングの直書きから共通トークンへ段階的に移行する。動きを減らすOS設定とアプリ設定では全トークンを1msへ縮退する。 | P47、動きを減らす設定確認 |
| モバイルナビ | ボトムナビの選択面を、チームカラー追随のLiquid Lens面、短いブランドライン、アイコン・ラベルの強調で構成した。押下時は80msの控えめな沈み込みと一度だけのキネティック反応を持つ。 | 既存の5/4タブ、経路、ARIAラベル、44px親指操作領域、team color・light/dark追随を維持する。 | P47、P38、P54、実機確認 |
| モーダル・コンテキストバー | `modal-mobile-more`を含むモーダルに開閉状態を追加し、シートは260msで表示・180msで閉じる。Tab循環、Escape、閉じた後のトリガーへのフォーカス復帰を共通化した。コンテキストバーも200ms/140msへ統一した。 | DOM ID、既存の`openModal`呼び出し、その他の導線、role切替、同期操作、即時のルート状態更新を維持する。 | P47、P54、P37、キーボード確認 |
| PWA・統制 | cache世代を`coachmgr-v206`へ進め、モーション・選択面・動き削減・フォーカストラップの再発防止契約を追加した。 | 更新通知でCSS・JSの新しいモーション資産を既存端末へ配信する。 | P47、P49、P54、全契約テスト |

## v1.30.95 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| 状態色 | サイドバー、モバイルボトムナビ、ロール切替、同期状態、予定チップの固定赤・緑・青をbrand、success、danger、info、selected surfaceの意味トークンへ移行した。 | 既存のナビ経路、ロール判定、同期状態クラス、予定ラベルを維持し、任意のチームカラーとlight/darkへ追随する。 | P47、P34、テーマ検証 |
| 非標準操作 | `c-modal__close`、`c-data-list__item--button`、`c-status--interactive`へhover、focus-visible、active、disabledの最小状態契約を追加した。PK戦削除は未参照`c-static-style--006`から`c-icon-button--danger`へ移行した。 | 既存のDOM ID、`btn-side-remove-pk`イベント、モーダル閉じ、行遷移、フィルタ解除を維持する。 | P47、P39、アクセシビリティ確認 |
| 形状・影 | dashboard、drawing、tactical、componentsの通常surfaceにある直書き半径を`radius-sm`、`radius-md`、`radius-lg`、`radius-pill`へ統一し、基本shadowをテーマ追随のcolor-mixトークンへ移行した。 | ピッチ・進捗・スクロールバーの視覚的に必要な例外は`UI_EXCEPTION_LEDGER.md`へ明文化する。 | P47、P40、レスポンシブ検証 |
| 情報階層 | ダッシュボードの予定・実績空状態から重複した「試合を追加」を除去し、上部カードの唯一の主操作へ誘導を集約した。 | 試合追加モーダル、予定・実績の集計、保護者／コーチの表示制御を維持する。 | P47、P38 |
| PWA・統制 | cache世代を`coachmgr-v205`へ進め、P47で状態色、操作状態、半径、例外台帳の再導入を防止する。 | 更新通知により既存端末へ最新UI資産を一括配信する。 | P47、P49、P54、全契約テスト |

## v1.30.94 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| PWA更新通知 | 緑の更新通知内で赤いprimaryボタンが表示される混在を解消した。更新操作は成功状態専用のsurface・text・hover・pressedトークンを使用し、通常・ホバー・押下を同系色で連続させる。 | `btn-pwa-reload`、更新通知の表示・閉じる・Service Worker更新フローを維持する。 | P47、PWA公開確認 |
| フォーカスと影 | 更新操作のfocus-visibleリングと影を成功トーンへ限定し、チームの赤いprimary影・hoverが漏れないようにした。 | 汎用primaryボタンの通常のチームカラー表現は変更しない。 | P47、キーボード確認 |
| PWA・統制 | cache世代を`coachmgr-v204`へ更新した。 | 更新通知により状態色修正を既存利用者へ配信する。 | P47、P49、全契約テスト |

## v1.30.93 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| 練習カード操作 | 一般コンテナクエリが共有操作の`grid-column`を`auto`へ戻し、内容幅まで縮めていた競合を解消した。カード専用の高詳細度グリッドにより、1段目を2等分、2段目の共有・編集・削除を3等分へ固定した。 | 既存の操作クラス、DOM ID、イベント、ARIAラベル、主・補助操作の順序を維持する。 | P49、P54、公開PWA確認 |
| 操作面の均整 | すべての練習操作へ統一した44px以上の高さ、余白、中央配置、マージン0を適用し、補助操作を一体的に表示する。 | 共有・編集・削除の可視ラベルと危険操作の色役割を維持する。 | P49、P54、レスポンシブ検証 |
| PWA・統制 | cache世代を`coachmgr-v203`へ更新した。 | 更新通知により高詳細度グリッドを既存利用者へ配信する。 | P47、P49、P54、全契約テスト |

## v1.30.92 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| モバイルその他メニュー面 | 共通`c-modal--bottom-sheet`と`c-mobile-more__sheet`の上端左右を`--radius-lg`で明示し、未定義トークンにより全角が0pxとなる事象を解消した。surfaceの境界線・shadow・下端フラットは維持する。 | `modal-mobile-more`、Esc・overlay閉じ、ロール切替、同期、経路ボタンを維持する。 | P49、P54、公開PWA確認 |
| 練習カード操作 | 2段構成を維持したうえで、1段目を2等分、2段目を共有・編集・削除の3等分へ統一した。編集・削除は可視ラベルを追加し、全操作を同じ44px以上の高さ・中央配置に揃えた。 | 既存の操作クラス、DOM ID、イベント、ARIAラベルを維持する。 | P49、P54、レスポンシブ検証 |
| PWA・統制 | cache世代を`coachmgr-v202`へ更新した。 | 更新通知により角丸surfaceと均等操作レイアウトを配信する。 | P47、P49、P54、全契約テスト |

## v1.30.91 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| 練習カード操作 | 添付実画面を基準に5操作一列を撤廃した。1段目はメニュー・テンプレート、2段目は共有・編集・削除として固定し、共有の圧縮をなくした。 | 既存の操作クラス、DOM ID、イベントを維持する。 | P49、P54、レスポンシブ検証 |
| ボトムナビその他 | `aria-expanded`と`is-expanded`をモーダル状態と同期し、展開中も他タブと同じ8px丸角・選択面を使用する。 | `modal-mobile-more`と既存モーダル開閉、Esc、overlay閉じ、経路遷移を維持する。 | P38、P54、公開PWA確認 |
| PWA・統制 | cache世代を`coachmgr-v201`へ更新した。 | 更新通知により操作レイアウトとナビ外観を配信する。 | P47、P49、P54、全契約テスト |

## v1.30.90 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| ロール別情報階層 | 保護者モードではコーチ専用経路を含む「チーム管理」セクション全体を非表示にし、空の見出しを残さないようにした。 | `mobile-more-navigation-section`を追加し、既存の`coach-only`経路・ロール切替・同期導線を維持する。 | P54、公開PWA確認 |
| PWA・統制 | cache世代を`coachmgr-v200`へ更新した。 | 更新通知により保護者表示補正を配信する。 | P47、P49、P54、全契約テスト |

## v1.30.89 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| 練習カード操作 | 文言付きのメニュー・テンプレート・共有を先頭3列へ配置し、編集・削除は44pxの補助操作へ分離した。主操作には最大幅を、共有には収縮可能な幅を与え、超狭幅では3列へフォールバックする。 | `btn-add-menu`、`btn-save-practice-template`、`btn-share-practice`、編集・削除のDOMクラス・イベントを維持する。 | P38、P49、P54、レスポンシブ検証 |
| モバイルその他 | その他メニューを共通`c-modal--bottom-sheet`上の単一surfaceとして構成し、表示モード・同期・チーム管理・表示設定を意味セクションへ整理した。 | 既存の`modal-mobile-more`、ロール切替、同期、経路ボタン、テーマ、リリースノートのIDとイベントを維持する。 | P38、P54、アクセシビリティ確認 |
| ボトムナビ | buttonであるその他タブの既定枠線・背景を共通`c-bottom-nav__item`規則で除去し、他タブと同じ選択面と親指操作領域に統一した。 | タブ5項目、権限制御、既存`data-route`を維持する。 | P38、P54、レスポンシブ検証 |
| PWA・統制 | cache世代を`coachmgr-v199`へ更新し、P54を追加した。 | 更新通知により既存利用者へCSS・テンプレート・JS資産を一括配信する。 | P47、P49、P54、全契約テスト |

## v1.30.88 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| 直接指定色 | 画面・部品層の直接HEX／RGBA119件をトークンと`color-mix()`へ移行し、監査対象を0件にした。 | `base.css`と`tokens.css`の色値はテーマ・Canvas・スウォッチの所有値として維持する。 | P34、P50、P53、色債務監査 |
| ダッシュボード | アラート、予定・結果、ヒートマップ、hover・装飾面をbrand、状態、heatmap役割トークンへ移行した。 | 既存の表示ラベル、ランキング構造、保護者閲覧制御を維持する。 | P34、P41、P45、レスポンシブ検証 |
| 共通部品 | 参加者チップ、メディアプレビュー、フレーム選択、ピッチトークン、popover、練習カードの色役割を共通トークンへ統合した。 | DOM ID、イベント、作図・動画export・モーダルの既存導線を維持する。 | P40、P45、P46、P51、P52 |
| 静的カタログ | `c-static-style--*`内の色見本、surface、media、shadow、区切りを基盤トークンへ移行した。 | 既存テンプレートのクラス参照は維持し、段階的な命名移行を別管理とする。 | P35、P44、P46、P47、P53 |
| PWA・統制 | cache世代を`coachmgr-v198`へ更新し、P53を追加した。 | 更新通知と初回オフライン起動で最新CSS資産を配信する。 | P47、P53、全契約テスト |

## v1.30.87 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| Canvasパレット | `canvas-palette.js`を唯一のCanvas色解決層とし、ピッチ、ガイド、選手、器具、選択、軌跡、exportを意味トークンへ接続した。 | 保存済みの`red`／`blue`等と任意HEXは変換せず、描画直前に解決する。既存の作図データ、undo/redo、フォーメーション一括配置、画像・動画exportを維持する。 | P40、P43、P51、テーマ検証 |
| 作図・戦術UI | `drawing.css`と`tactical.css`の直接指定色92件を除去し、Canvasトークンへ集約した。 | light/darkにおけるピッチ、線、選択ハンドル、文字、操作面の視認性を役割単位で維持する。 | P34、P40、P51、レスポンシブ検証 |
| レガシー画面 | `components.css`の直接指定色66件を状態・surface・shadowトークンへ移行した。 | RSVP、インサイト、PWA更新通知、注意表示、動画面、ピッチプレビューのDOM IDとイベントを維持する。 | P45、P49、P52 |
| モーダル | 未参照となった旧`.modal`、`.modal-overlay`、`.bottom-sheet-modal` shell規則を削除し、`c-modal--bottom-sheet`を共通シェルに追加した。 | 既存の`modal`補助クラスは内容レイアウト用として維持し、表示面・overlay・閉じ遷移は`c-modal`が一元管理する。 | P49、P52、PWA更新確認 |
| PWA・統制 | `canvas-palette.js`と`pitch-renderer.js`をprecacheへ追加し、cache世代を`coachmgr-v197`へ更新した。P51・P52を追加した。 | 初回オフライン起動と更新通知の資産整合を維持する。 | P47、P49、P51、P52、全契約テスト |
| 色負債 | 画面・部品層の直接指定色を277件から119件へ削減した。 | 基盤トークンと`canvas-palette.js`内の意図的なフォールバックは監査対象から分離する。 | P50、色負債監査 |

## v1.30.86 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| モーダル | 旧`.modal`／`.modal-overlay`規則を未移行マークアップへ限定し、`c-modal`と`c-modal--legacy`のsurface、overlay、radius、shadow、header、actionsを共通シェルへ統一した。 | DOM ID、dialog、既存イベント、`c-modal--legacy`の内容構造は維持する。 | P35、P45、P49、PWA更新確認 |
| ランキング | 種別ラベルを左端、数値を右端へ置く二列指標gridに変更した。指標はカード幅の46%以下で収縮する。 | 得点、アシスト、出席率、出場率の既存`c-data-list`マークアップを維持する。 | P41、P49、レスポンシブ検証 |
| 練習操作 | `メニュー`、`テンプレ`、`共有`を狭いカードではヘッダー下の3列、編集・削除を44px操作へ分離し、ellipsisを廃止した。 | ボタンID、クラス、クリックイベント、保護者モードの共有導線を維持する。 | P38、P49、高密度プロトコル |
| 色負債 | モーダルoverlayを`--color-overlay-scrim`へ移行し、画面・部品の直接指定色を278件から277件へ削減した。47件削減の基準比較と残債内訳を再生成可能なレポートへ記録した。 | 基盤トークン、Canvas・戦術配色、静的テンプレートは分類・移行順を維持する。 | P45、P50、色負債分析 |

## v1.30.85 — 2026-08-20

| 領域 | 変更 | 移行・互換性 | 検証 |
|---|---|---|---|
| カラー | `--color-status-*`、通知、オーバーレイ、toastの役割トークンを追加した。ダッシュボードの主要なブランド・予定・結果・進捗表現をセマンティックカラーへ移行した。 | 旧`--primary`系の参照は段階移行とし、新規部品では使用しない。 | P34、P44、P45、デザインシステム監査 |
| 状態部品 | `c-notice`、`c-state-icon`、状態別toast、入力invalid、button disabled/loading、状態ラベルの押下状態を標準化した。 | `showToast(message)`は従来どおり使える。第2引数で`type`と`duration`を任意指定できる。 | P45、P46、P47 |
| アクセシビリティ | トーストをlive region、確認モーダルを名前・説明付きdialogへ更新した。 | 既存DOM IDと既存の確認APIは維持する。 | P45、P47 |
| アイコン配信 | 実行時144クラスのTablerサブセットを採用し、PWA precacheをフル資産から切り替えた。 | 新しい`ti-*`を追加・削除するPRでは、サブセットの再生成が必須。 | P42、サブセット評価 |
| 高密度検証 | 12名、長い日本語名、未回答、複数操作を含む固定フィクスチャと視覚回帰プロトコルを追加した。 | 実ユーザーデータを上書きしない。 | P46、P47、手動プロトコル |
| 統制 | デザインシステム監査、PRテンプレート、変更履歴、品質プロトコルを追加した。 | UI変更はテンプレートと品質ゲートを通す。 | P44、P47、`git diff --check` |

## 記載規約

| 項目 | 必須記載内容 |
|---|---|
| バージョン | アプリ版、日付、変更したデザインシステム領域 |
| 変更 | 新しい契約、変更した契約、非推奨化、削除予定 |
| 移行 | 影響する画面、DOM・イベントの互換性、移行期限、例外 |
| 検証 | 追加・変更した契約テスト、テーマ・レスポンシブ・PWA確認 |
| 解除条件 | 一時的例外がある場合、その削除条件と担当フェーズ |
