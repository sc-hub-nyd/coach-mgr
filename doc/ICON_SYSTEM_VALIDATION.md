# 南陽台FCアイコンシステム：検証記録

## 資産と実装

提供パッケージに含まれる44個のSVGを、カテゴリ名とファイル名を維持したまま`assets/icons/nanyodai/`へ格納した。`CSS/icon-system.css`はCSSマスクを使い、親要素の`color`を継承する`c-icon`部品として描画する。これにより、SVGを複製せずにlight/dark、チームテーマ、主操作面の前景色へ対応する。

## 視覚確認

2026-08-18にChromium headlessで画面を確認した。320px幅では、ホーム、予定、試合、選手のアイコンを用いたボトムナビゲーションがラベルと一緒に1行へ収まり、操作領域の横方向オーバーフローは発生しなかった。ラベルを併記しているため、アイコンを認識しづらい利用者にも経路を理解できる。

1440px幅では、チームシグナルを用いたブランド表示と、ホーム、トロフィー、コーン、上昇パス、ドキュメント、パスラダー、チームのサイドナビゲーションが統一された線幅・前景色で表示された。アイコンはナビゲーションの主情報である日本語ラベルを補助し、状態や意味を色だけに依存していない。

## 自動検証結果

| 検証 | 結果 |
|---|---|
| アイコン資産 | 44 / 44 SVGを確認 |
| CSSアーキテクチャ契約 | 合格 |
| 動的テーマコントラスト | 14種色 × light/darkで合格 |
| レスポンシブ検証 | 5ビューポート × 5画面、25チェック、違反0件 |

## PWA

`icon-system.css`とアプリシェルで使う主要な9個のSVGを`coachmgr-v70`のプリキャッシュへ加えた。初回インストール後も、主要ナビゲーションとブランドアイコンはオフラインで利用できる。
