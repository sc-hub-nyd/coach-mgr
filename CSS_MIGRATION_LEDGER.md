# CoachMgr CSS移行台帳

**基準日：2026-08-18**
**対象リリース：v1.29.2**
**目的：画面固有CSSを共通部品へ移す対象、ドメイン固有として残す対象、監査済みで削除する対象を根拠とともに追跡する。**

## 1. 運用規則

この台帳は、単にCSSを減らすための削除リストではありません。規則を削除する前に、静的な非CSS参照、DOM契約、置換先部品、対象画面の機能スモークを確認します。JavaScriptが計算する座標・寸法・表示状態、試合中の安全操作、キャンバス操作は、参照数だけで削除しません。

| 状態 | 意味 | 次のアクション |
|---|---|---|
| `共通化待ち` | 同型のUIが残り、`c-*`部品へ寄せられるが、まだ対象変更単位に着手していない。 | 置換先、DOM契約、検証範囲を決めて次の機能変更と同時に移行する。 |
| `完了` | 共通部品への移行または監査済み削除が済んだ。 | 契約テストと台帳で再発を防ぐ。 |
| `固有維持` | 描画・試合中操作・OS互換など、一般化すると危険または意味が薄い。 | 残す理由と対象契約を維持する。 |
| `使用中` | 静的な非CSS参照があり、現時点で削除できない。 | 次の機能変更時に置換先を評価する。 |
| `要調査` | 動的生成、旧互換、外部要因により影響範囲が未確定。 | 実行時表示とイベント接続を確認してから分類する。 |

## 2. 波6の監査結果

監査スクリプト `scripts/audit-legacy-css.sh` は、`CSS/components.css` の `u-ext-*` 定義と、CSS以外のプロジェクトファイルにある同名クラス参照を再計測します。監査結果は `reports/legacy-css-audit.tsv` に保存します。

| 指標 | 実測値 | 判断 |
|---|---:|---|
| 監査対象の `u-ext-*` 定義 | 166 | 残存規則には静的な非CSS参照があり、自動削除しない。 |
| 非CSS参照0件の候補 | 0 | 波6で監査対象の未使用候補をすべて削除済み。 |
| 削除した規則 | 39 | `u-ext-27〜37`、`58〜60`、`99〜103`、`110〜111`、`115〜122`、`141`、`144〜149`、`168〜170`、`184`。 |
| 監査・削除リリース | 3 | v1.29.0、v1.29.1、v1.29.2として独立公開。 |

> **削除判断の境界**：この監査は静的参照の存在を確認するための安全ゲートです。`u-ext-*` の残存数をゼロにすることは目的にせず、機能変更を伴わずに使用ゼロと証明できた規則だけを削除します。

## 3. ファイル別台帳

| ファイル | 実測行数 | 主な責務 | 状態 | 現在の扱い |
|---|---:|---|---|---|
| `CSS/base.css` | 1,499 | アプリシェル、固定ナビ、モバイル互換、旧画面補正 | 要調査 | アプリシェル、safe area、役割別表示を維持。画面固有補正は機能変更時に切り出す。 |
| `CSS/components.css` | 3,745 | 旧ボタン、フォーム、モーダル、一覧、互換規則 | 使用中 | 波6で監査済み未使用規則を削除。残る旧規則は非CSS参照を確認済みのため、置換と同時に削除する。 |
| `CSS/dashboard.css` | 1,945 | ホーム導線、ランキング、KPI、役割別表示 | 固有維持 / 使用中 | KPI、ランキング、空状態は共通化済み。ホーム固有の役割分岐は維持。 |
| `CSS/tactical.css` | 319 | 戦術一覧・ピッチプレビュー | 固有維持 / 使用中 | 検索・カード・編集導線は標準化済み。ピッチプレビューは画面固有として維持。 |
| `CSS/drawing.css` | 1,511 | 作図ツール、キャンバス、アニメーション操作 | 固有維持 / 使用中 | キャンバス、座標、ドラッグ、Undo/Redoを維持。フィルムストリップ・出力導線は標準化済み。 |
| `CSS/components-standard.css` | 331 | 汎用面、見出し、操作群、空状態、サーフェス | 移行先 | 一般構造・余白・状態は原則ここへ追加する。 |
| `CSS/components-system.css` | 1,592 | フォーム、名簿、練習カード、一覧、モーダル | 移行先 | 通常業務画面のフォーム・一覧・モーダル契約を集約する。 |

## 4. パターン別の現状

| パターン | 状態 | 置換先または残存理由 | 保護する契約 |
|---|---|---|---|
| 旧`.card`と画面固有カード補正 | 使用中 | `c-card`、`c-section-header`、`c-liquid-panel`へ主要画面は移行済み。役割別導線は個別に維持。 | 詳細リンク、role別表示、クリックID。 |
| 旧`.form-group`と裸`label` | 使用中 | `c-form-field`、`c-fieldset`へ主要フォーム・モーダルは移行済み。静的参照が残る箇所は個別移行時に処理。 | `for`/`id`、必須、エラー、保存フォーム。 |
| 旧`.modal` / `.bottom-sheet-modal` | 使用中 | `c-modal`、必要時`c-confirm-dialog`へ主要モーダルを移行済み。Field Companionの試合中シートは安全優先で維持。 | 開閉ID、フォーカス戻し、footer操作、保存・破壊操作。 |
| `u-ext-*`拡張クラス | 使用中 | 166定義は静的参照がある。波6で使用ゼロの39規則を削除済み。 | JSの動的付与、表示分岐、既存セレクタ。 |
| `style="..."` | 要調査 | 固定の反復指定は波1〜5で共通部品へ移行。キャンバス座標、動的寸法、表示状態は理由付きで維持。 | 座標、幅、高さ、transform、表示切替。 |
| ランキング・KPI行 | 完了 | `c-data-list`、`c-metric-grid`、`c-metric`。 | 数値精度、順位、単位、比較可能な列。 |
| 履歴・イベント行 | 完了 | `c-data-list`、必要時`c-roster-row`、`c-action-group`。 | 時刻順、イベントID、編集・削除。 |
| Field Companionの主操作 | 固有維持 / 完了 | クイック操作・名簿・ログは共通契約へ接続。ライブ記録面は安全優先の専用CSSを維持。 | 44px以上、片手操作、オフライン記録、連続操作。 |
| 作図キャンバス・ドラッグ領域 | 固有維持 | 通常UIだけを共通化し、キャンバス座標・スナップ・Undo/Redo・描画・出力を維持。 | 座標、スナップ、Undo/Redo、描画・出力。 |

## 5. 検証ゲート

| ゲート | 波6での確認内容 |
|---|---|
| 静的使用監査 | `scripts/audit-legacy-css.sh`実行後、`reports/legacy-css-audit.tsv`の非CSS参照0件候補が0件であること。 |
| P33 | CSSアーキテクチャと設計トークンの契約。 |
| P34 | 15種の種色 × light/darkの動的テーマ・コントラスト。 |
| P35 | DOM ID、主要な共通部品構造、移行済み画面の回帰防止。 |
| レスポンシブ | 5画面 × 5ビューポートの自動検証で違反0件。 |
| 差分健全性 | `git diff --check`で空白・競合・形式上の問題がないこと。 |

## 6. 次回の削除ルール

1. 既存規則の削除前に、監査スクリプトを実行し、CSS以外の参照が0件であることを確認する。
2. 参照がある規則は削除せず、対応するHTMLまたはJavaScriptの標準部品移行と同じコミットで置換する。
3. 作図・アニメーションの座標、試合中の記録操作、同期・復元の動的表示は、静的参照がなくても実行時契約を確認する。
4. 削除ごとにP33、P34、P35、レスポンシブ検証、`git diff --check`を通過させる。
5. 台帳・監査結果・リリースノートを同じ公開単位で更新し、再発防止の根拠を残す。
