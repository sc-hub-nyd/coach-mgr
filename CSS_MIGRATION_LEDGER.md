# CoachMgr CSS移行台帳

**基準日：2026-08-18**
**対象リリース：v1.27.0**
**目的：画面固有CSSを、共通部品へ移す対象・ドメイン固有として残す対象・使用ゼロ確認後に削除する対象へ分離する。**

## 1. 運用規則

この台帳は、CSS削除のチェックリストではなく、**置換先と残す理由を追跡する設計記録**です。規則を削除する前に、利用箇所、DOM契約、置換先部品、対象画面の機能スモークを確認します。

| 状態 | 意味 | 次のアクション |
|---|---|---|
| `共通化待ち` | 同型のUIが複数画面に残り、`c-*`部品へ寄せられる。 | 対象の移行波でHTMLとCSSを置換する。 |
| `固有維持` | 描画・試合中操作・OS互換など、一般化すると危険または意味が薄い。 | 残す理由と対象契約を維持する。 |
| `削除候補` | 置換先があり、使用箇所の削除確認だけが残る。 | 使用ゼロ・全検証後に単独コミットで削除する。 |
| `要調査` | JS動的生成、旧互換、外部要因により影響範囲が未確定。 | 利用箇所と実行時表示を調査してから分類する。 |

## 2. ファイル別台帳

| ファイル | 規模 | 主な責務 | 状態 | v2の扱い |
|---|---:|---|---|---|
| `CSS/base.css` | 1,499行 | アプリシェル、固定ナビ、モバイル互換、旧画面補正 | 要調査 | アプリシェル・safe area・役割別表示は維持。ダッシュボードや練習固有補正は波1・6で移管する。 |
| `CSS/components.css` | 4,000行 | 旧ボタン、フォーム、モーダル、一覧、互換規則 | 共通化待ち | 既存`c-form-field`、`c-modal`、`c-action-group`へ段階移管。使用ゼロの旧規則を波6で削除する。 |
| `CSS/dashboard.css` | 1,945行 | ホーム導線、ランキング、KPI、役割別表示 | 共通化待ち | 波1で数値行・履歴・判断カード・空状態を標準部品へ寄せる。ホーム固有の分岐は残す。 |
| `CSS/tactical.css` | 319行 | 戦術一覧・ピッチプレビュー | 共通化待ち / 固有維持 | 検索・カード・編集導線は標準化。ピッチプレビューは画面固有として残す。 |
| `CSS/drawing.css` | 1,511行 | 作図ツール、キャンバス、アニメーション操作 | 固有維持 / 共通化待ち | キャンバス、座標、ドラッグは維持。ツールバー、設定、確認UIは波5で移管する。 |
| `CSS/components-standard.css` | 216行 | 汎用面、見出し、操作群、空状態、サーフェス | 移行先 | 新たな一般構造・余白・状態は原則ここへ追加する。 |
| `CSS/components-system.css` | 965行 | フォーム、名簿、練習カード、リスト、モーダル | 移行先 | 通常業務画面のフォーム・一覧・モーダル契約をここへ集約する。 |

## 3. 既知のレガシーパターン

| パターン | 出現傾向 | 分類 | 置換先 | 移行波 | 保護する契約 |
|---|---:|---|---|---:|---|
| 旧`.card`と画面固有カード補正 | 多数 | 共通化待ち | `c-card`、`c-section-header`、`c-liquid-panel` | 1、3、6 | 詳細リンク、role別表示、クリックID。 |
| 旧`.form-group`と裸`label` | 約46箇所 | 共通化待ち | `c-form-field`、`c-fieldset` | 2、3、5 | `for`/`id`、必須、エラー、保存フォーム。 |
| 旧`.modal` / `.bottom-sheet-modal` | 多数 | 共通化待ち | `c-modal`、必要時`c-confirm-dialog` | 2、3、5 | 開閉ID、フォーカス戻し、footer操作、保存・破壊操作。 |
| `u-ext-*`拡張クラス | 約276件 | 要調査 | 標準部品の要素・修飾子、または画面固有名 | 1、3、6 | JSの動的付与、表示分岐、既存セレクタ。 |
| `style="..."` | `index.html`に469件 | 要調査 / 共通化待ち | トークン、`l-*`、`c-*`、限定的な動的style | 0〜6 | JSが更新する寸法、座標、表示状態。 |
| Font Awesome・絵文字アイコン | 画面ごとに残存 | 共通化待ち | `c-icon`、単独操作の`aria-label` | 1〜5 | 意味、状態、アクセシビリティラベル。 |
| ランキング・KPI行 | ダッシュボード、選手、振り返り | 共通化待ち | `c-data-list`、必要時`c-metric-row` | 1 | 数値精度、順位、単位、比較可能な列。 |
| 履歴・イベント行 | 試合、振り返り、同期 | 共通化待ち | `c-data-list`、必要時`c-timeline-item` | 1、3、4 | 時刻順、イベントID、編集・削除。 |
| 作図キャンバス・ドラッグ領域 | 作図・アニメーション | 固有維持 | 専用CSS | 5 | 座標、スナップ、Undo/Redo、描画・出力。 |
| Field Companionの得点・失点・交代操作 | 試合当日 | 固有維持 | 意味トークン、`c-icon`、専用操作面 | 4 | 44px以上、利き手、オフライン記録、連続操作。 |

## 4. 画面別の初期移行バックログ

| 画面 | 最初に移すパターン | 置換先 | リスク | 受入の焦点 |
|---|---|---|---|---|
| ダッシュボード | ランキング、KPI、予定・履歴、role別空状態 | `c-data-list`、`c-card`、`c-empty-state` | 中 | コーチ／保護者の表示分岐と詳細遷移。 |
| 選手管理 | 成績比較、参加履歴、編集モーダル | `c-data-list`、`c-modal`、`c-form-field` | 中 | 長い選手名、CSV、詳細遷移。 |
| 振り返り | KPI、比較行、履歴行、判断カード | `c-data-list`、`c-card`、`c-action-group` | 低〜中 | 数値・根拠・対象選手の分岐。 |
| メニュー・戦術 | 作成／編集モーダル、一覧カード | `c-modal`、`c-form-field`、`c-card` | 中 | カテゴリ、タグ、作図遷移。 |
| 試合詳細 | 招集、タイムライン、スコア周辺 | `c-roster-row`、`c-data-list`、`c-action-group` | 高 | 選手ID、時系列、得点計算。 |
| Field Companion | 出場中・ベンチ、試合ログ、保存状態 | `c-roster-row`、`c-data-list`、状態トークン | 高 | 片手操作、連続記録、オフライン。 |
| 作図・アニメーション | ツールバー、設定、フレーム操作、確認UI | `c-action-group`、`c-form-field`、`c-modal` | 高 | キャンバス座標、Undo/Redo、出力。 |

## 5. 更新手順

1. 対象画面の実装前に、該当行の状態を`共通化待ち`から`進行中`へ変更する。
2. 置換先、DOM契約、比較対象のビューポート、手動スモーク項目を追記する。
3. 移行後にP33、P34、P35、レスポンシブ検証、対象機能テストを実行する。
4. 旧規則が使用ゼロなら`削除候補`へ更新し、削除コミットを別に切る。
5. 画面固有として残す規則は、理由と再評価時期を記録する。

## 6. 波0完了条件

- 本台帳と`INLINE_STYLE_MIGRATION_REGISTER.md`が存在し、各画面に最初の移行パターンが割り当てられている。
- P33とP35に、台帳で定義した新規部品候補・対象画面の契約追加方針が反映されている。
- 次の波で着手するダッシュボード、選手、振り返りについて、比較対象と機能スモークが定義されている。
