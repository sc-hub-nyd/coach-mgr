# v1.30.91 公開PWA検証記録

- 確認日時: 2026-08-20 (GMT+9)
- 公開URL: GitHub Pages公開URL
- 対象コミット: `90697c1ddacfbb780f16d7659feb1e612bae9ce1`

## 更新配信状態

初回表示では、既存のService Workerキャッシュを利用していたためバージョンバッジが `v1.30.90` と表示された。`registration.update()` の結果、`waiting` Service Worker が検出された。これは、v1.30.91（`coachmgr-v201`）がインストール済みで、旧ページの利用を妨げずに更新を適用できる状態であることを示す。

## 次の確認手順

更新通知の「更新する」を実行して新しいService Workerを有効化した後、再読み込みしてバージョンバッジが `v1.30.91` になることを確認する。その後、練習管理の操作が2段（メニュー・テンプレート／共有・編集・削除）であること、およびボトムナビ「その他」が展開中に8pxの丸角と選択面を示すことを確認する。

## CI結果

- Contract tests: 成功（GitHub Actions run `32330454693`）
- Pages build and deployment: 成功（GitHub Actions run `32330454060`）
- ローカル全品質ゲート: 契約テスト45/45、レスポンシブ20件、テーマ30通り、画面・部品層の直接指定色0件

## 更新適用結果

更新通知の「更新する」を選択した結果、ページは安全に再読み込みされ、バージョンバッジが `v1.30.91` へ更新された。これにより、新しい `coachmgr-v201` Service Workerと公開資産が利用されていることを確認した。再読み込み後はコーチモードの既存セッション状態が復元され、クラウド同期の初期化表示が開始された。

次に、同期完了後の練習管理画面とモバイル幅のボトムナビで実画面の見た目を確認する。

## 公開画面確認

v1.30.91への更新後、公開PWAで練習管理画面を開き、バージョンバッジが `v1.30.91` のまま維持されることを確認した。検証ブラウザには練習データが存在しないため、実データの練習カード自体は表示されなかった。このため、カードの二段配置はローカルのP49/P54契約・レスポンシブ検証と、公開配信済みCSSの規則照合で担保する。実チームデータを持つ利用端末では、更新通知を適用した後に表示確認を行う。

## 公開資産の直接照合

キャッシュバイパス付きのGitHub Pages資産を照合し、以下を確認した。

| 資産 | 確認結果 |
|---|---|
| `version.js` | `APP_VERSION = 'v1.30.91'` |
| `sw.js` | `CACHE_VERSION = 'coachmgr-v201'` |
| `CSS/components-system.css` | `grid-template-columns: repeat(6, minmax(0, 1fr));` |
| `app.js` | `syncBottomNavMoreState` が `is-expanded` をモーダル状態と同期 |

公開資産、Service Worker更新通知、CIのいずれもv1.30.91を指していることを確認した。
