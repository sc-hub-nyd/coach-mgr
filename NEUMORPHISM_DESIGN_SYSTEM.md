# CoachMgr ニューモーフィズム設計規約

**文書版：1.0**

## 1. 目的

CoachMgrでは、ニューモーフィズムを装飾目的ではなく、**画面上の面・操作・状態の関係を穏やかな奥行きで伝えるための補助表現**として扱います。南陽台FCの赤系を起点とする任意チームカラー、ライト／ダークの二つの表示モード、既存の高密度な業務UIを維持し、可読性・キーボード操作・情報密度を損なわないことを優先します。

## 2. 基本原則

| 原則 | 規約 |
| --- | --- |
| 意味優先 | 色、アイコン、ラベル、明確な境界を残し、影だけに意味を負わせない。 |
| 制度設計 | 明部・暗部・浮面・凹面はすべてセマンティックトークンで定義し、画面固有の色や影を直接指定しない。 |
| 操作状態 | 通常は浮面、押下中・入力領域は凹面、フォーカスは既存の明確なフォーカスリングを優先する。 |
| アクセシビリティ | 影で区別する面にも境界線を残す。文字コントラストとフォーカス可視性を低下させない。 |
| 高密度UI | 一覧、表、ログ、試合中操作では影を軽くし、空白や過大な角丸で情報量を犠牲にしない。 |

## 3. トークン

| トークン | 用途 |
| --- | --- |
| `--neo-highlight` | 光源側のハイライト。ライト／ダークモードで自動調整する。 |
| `--neo-shadow` / `--neo-shadow-soft` | 影側の濃度。任意チームカラーの文字色・サーフェスから派生する。 |
| `--neo-surface` | 浮面・選択面の基準サーフェス。 |
| `--shadow-neo-raised` | 通常の浮面。カード、補助ボタン、フィルターバーに使用する。 |
| `--shadow-neo-raised-hover` | ポインタ操作可能な浮面のホバー状態。 |
| `--shadow-neo-pressed` | 凹面。検索入力、メトリクスの内側面、補助操作の押下状態に使用する。 |

> ライト／ダークのニューモーフィズムは固定の白・黒影を画面へ直接記述せず、`tokens.css`のトークンだけを介して表現します。

## 4. 適用対象

`c-card`、`c-practice-card`、`c-data-list__item`、`c-filter-bar`、`c-choice-field`、および`btn-secondary`に浮面表現を適用します。検索ボックスとデータリストの小メトリクスには凹面表現を使用します。主操作ボタン、危険操作、通知バナー、Field Companionの得点・失点操作には、チームカラーや意味色による明確な状態識別を優先し、ニューモーフィズムを強制しません。

## 5. 禁止事項

- 影だけでクリック可能・選択済み・エラーを表現しない。
- `box-shadow`へ固定色を直接記述しない。
- フォーカスリングを影の見た目に置き換えない。
- 文字色・境界色・意味色のコントラスト要件を下げない。
- 試合中の得点、失点、交代、警告など、誤操作の影響が大きい主操作を装飾優先で弱めない。

## 6. 検証ゲート

ニューモーフィズムに関する変更は、P33 CSSアーキテクチャ契約、P34の15種色×ライト／ダークテーマ検証、P35の画面・DOM契約、5画面×5ビューポートのレスポンシブ検証をすべて通過してから公開します。

## 7. 今後の拡張

将来的に追加する部品は、まず標準`c-*`部品へニューモーフィズムを実装し、画面固有CSSへ複製しません。特殊な作図・試合中UIは、操作安全性を検証した後に限定導入します。
