# CoachMgr 画面固有CSSの共通部品移行ロードマップ v2

**策定日：2026-08-18**
**対象基準：CoachMgr v1.27.0**
**対象：Vanilla JS PWA、`CSS/main.css`配下のCSS、`index.html`、各レンダリングモジュール**

## 1. 目的

本ロードマップは、CoachMgrに残る画面固有CSSを、既存の`c-*`共通部品、`l-*`レイアウト、セマンティックトークンへ**機能を壊さず段階移行**するための実行計画です。目標はCSS行数の単純な削減ではなく、将来のテーマ変更、表示密度変更、ライト／ダーク調整、画面追加を、少数のトークンと共通部品だけで安全に反映できる状態を広げることです。

> **移行原則：データ・イベント・DOM契約を固定し、構造と見た目を小さな単位で共通化する。**

移行中に、イベントID、`data-*`属性、保存スキーマ、同期形式、画面遷移、試合イベントの並び順を変更しません。構造変更、見た目変更、データ処理変更を同一コミットへ混在させないことを必須とします。

## 2. 現状ベースライン

2026-08-18時点の再計測では、共通部品基盤は整備済みである一方、旧汎用CSS、ダッシュボード、作図、アプリシェルには画面固有の責務が残っています。

| 領域 | 現在規模 | 主な残存責務 | 方針 |
|---|---:|---|---|
| `CSS/components.css` | 4,000行 | 旧ボタン、フォーム、モーダル、一覧、互換スタイル | 共通部品へ順次吸収し、使用ゼロを確認した規則だけ削除する。 |
| `CSS/dashboard.css` | 1,945行 | ランキング、予定、KPI、ホーム導線、役割別表示 | 表示プリミティブを先に抽出し、画面分岐は維持する。 |
| `CSS/base.css` | 1,499行 | アプリシェル、ナビ、モバイル互換、旧カード補正 | アプリシェルは残し、画面固有の補正だけを移管する。 |
| `CSS/drawing.css` | 1,511行 | 作図ツールバー、キャンバス周辺、アニメーションUI | 描画エンジンは保持し、通常UIだけを共通部品へ寄せる。 |
| `CSS/components-system.css` | 965行 | フォーム、名簿、練習カード、データ一覧、モーダル | 移行先として拡張する。画面固有の例外を増やさない。 |
| `CSS/components-standard.css` | 216行 | カード、見出し、操作群、空状態、サーフェス修飾子 | 汎用の構造・余白・サーフェス契約を集約する。 |
| `index.html`のインラインスタイル | 469件 | テンプレート内の一回限りの余白・幅・表示補正 | 画面・パターン単位で削減する。全件一括置換はしない。 |

既存の移行計画（`COMPONENT_STANDARDIZATION_MIGRATION_PLAN.md`）に対し、設定、練習、一覧共通基盤、主要な空状態、ダッシュボード・振り返りの低リスク領域、試合詳細・Field Companion・作図のフォーム領域は移行済みです。v2では、残る**表示プリミティブ、旧モーダル、試合詳細の高リスク構造、作図専用UI、レガシー規則の削減**を対象にします。

## 3. 到達アーキテクチャ

変更は、必ず下記の方向へ向けます。

```text
チーム種色 / light・dark
        ↓
--theme-*（動的テーマプリミティブ）
        ↓
--color-* / --neo-* / --glass-* / --liquid-* / --surface-inset-*
        ↓
l-* レイアウト / c-* 共通部品
        ↓
画面固有クラス（ドメイン固有の状態・キャンバス・特殊レイアウトのみ）
```

### 3.1 継続利用する既存部品

| 部品 | 今後の主要な展開先 |
|---|---|
| `c-card`、`c-section-header`、`c-action-group` | ダッシュボード、選手カルテ、試合詳細の読書面、データ管理。 |
| `c-empty-state` | 役割別の空状態、検索結果なし、初期導線、分析結果なし。 |
| `c-form-field`、`c-fieldset` | 選手編集、試合編集、データ入出力、作図設定、確認フォーム。 |
| `c-modal` | 旧`.modal`、`.bottom-sheet-modal`のうち通常入力・確認用途。 |
| `c-filter-bar` | 試合、選手、メニュー、戦術、履歴画面の検索・フィルター。 |
| `c-data-list` | ランキング、選手比較、試合タイムライン、参加履歴、分析KPI。 |
| `c-roster-row` | 招集、出場中・ベンチ、出欠、選手状態。 |
| `c-icon` | 残るFont Awesome・絵文字の単独アイコン操作。 |
| `c-glass-surface`、`c-liquid-panel` | 固定ナビ、通常モーダル、ダッシュボードの文脈導線。 |

### 3.2 新規部品を作る条件

新規`c-*`部品は、次のすべてを満たす場合にだけ追加します。

1. **二つ以上の画面で同じ構造と状態を必要とする**。
2. 既存部品の修飾子では責務が曖昧になる。
3. DOM・アクセシビリティ・レスポンシブ規則を契約として記述できる。
4. 画面固有のデータ処理や座標計算を部品へ持ち込まない。

v2で追加候補とするのは、次の三つに限定します。

| 候補 | 想定する利用箇所 | 作成判断 |
|---|---|---|
| `c-metric-list` / `c-metric-row` | ランキング、KPI、選手比較、試合成績 | ダッシュボードと振り返り・選手で同じ数値構造を確認後に導入。 |
| `c-timeline-item` | 試合イベント、振り返り履歴、同期監査ログ | 時刻・種別・本文・操作という共通構造を二画面以上で確定後に導入。 |
| `c-confirm-dialog` | データ削除、復元、破壊的操作 | `c-modal`の構造を再利用できず、dangerロールと確認文言が反復する場合だけ導入。 |

## 4. 実行ロードマップ

ロードマップは、**合計12〜16週間相当**を目安とします。これは一人の開発者が、各波を小さなコミットに分け、既存の自動検証と手動スモークテストを行う前提の目安です。機能追加や同期仕様変更を同時に行う場合は、別のリリースラインとして扱います。

| 波 | 目安 | 中心テーマ | 主対象 | 主要成果 |
|---:|---:|---|---|---|
| 0 | 1週 | 移行の可視化と凍結 | レガシー規則・インライン指定・契約 | 画面別台帳、削除候補一覧、比較基準。 |
| 1 | 2週 | 表示プリミティブ | ダッシュボード、選手、振り返り | KPI、ランキング、履歴、役割別空状態の共通化。 |
| 2 | 2週 | フォーム・モーダル統合 | 選手、メニュー、戦術、データ管理 | 旧フォーム群・通常モーダル・確認導線の標準化。 |
| 3 | 2〜3週 | 試合詳細・招集・タイムライン | 試合詳細、招集、試合ログ、スコア周辺 | 高リスクの通常試合画面を構造単位で移行。 |
| 4 | 2〜3週 | Field Companion | 試合中操作、出場中・ベンチ、保存状態 | 片手操作・オフライン安全性を守る限定共通化。 |
| 5 | 2〜3週 | 作図・アニメーション | ツールバー、設定、フィルムストリップ、確認 | 描画エンジンを触らず通常UIを抽出。 |
| 6 | 1〜2週 | 削除・運用定着 | 旧CSS、インライン指定、文書、テスト | 未使用規則の削除と完了基準の確定。 |

## 5. 波0：移行台帳と比較基準を更新する

### 対象

`components.css`、`base.css`、`dashboard.css`、`drawing.css`、各画面テンプレート、動的HTML生成の`style="..."`、`u-ext-*`、旧`.card`、旧`.modal`、旧`.form-group`を対象にします。

### 実施タスク

| タスク | 実施内容 | 成果物 |
|---|---|---|
| セレクタ台帳 | 各セレクタを「共通化」「画面固有として保持」「削除候補」「要調査」へ分類する。 | `CSS_MIGRATION_LEDGER.md` |
| インライン指定台帳 | 469件を画面・理由・置換先候補で整理する。動的幅やキャンバス座標は除外理由を残す。 | `INLINE_STYLE_MIGRATION_REGISTER.md` |
| 比較基準 | ダッシュボード、選手、試合詳細、Field Companion、作図を320/390/768/1024/1440pxで記録する。 | 画面別スクリーンショットと検証ログ |
| 契約確認 | P33/P35の対象漏れを確認し、新規部品候補の最低契約を記述する。 | テスト拡張方針 |

### 受入条件

- 旧セレクタを「削除できそう」という印象で消さず、利用箇所と置換先を一対一で追跡できる。
- 画面固有として残す描画・座標・試合中安全UIには、残す理由が記録される。
- 波1以降は、対象画面の旧規則を台帳から選び、置換後に状態を更新する。

## 6. 波1：表示プリミティブを共通化する

### 優先理由

ダッシュボード、選手、振り返りには、KPI、ランキング、履歴、判断カード、空状態という同型の表示パターンが残ります。データ保存や試合中操作へ触れずに再利用効果が大きいため、高リスク画面より先に共通基盤を完成させます。

### 移行順序

| 順序 | パターン | 対象画面 | 置換先 | 変更しないもの |
|---:|---|---|---|---|
| 1 | 数値KPI・ランキング行 | ダッシュボード、選手、振り返り | `c-data-list`。必要なら`c-metric-row`を試作。 | 集計式、順位、成績データ。 |
| 2 | 予定・履歴・イベント行 | ダッシュボード、振り返り、選手参加履歴 | `c-data-list`。二画面で構造が一致したら`c-timeline-item`。 | 日付順、遷移先、イベントID。 |
| 3 | 判断・詳細リンク付きカード | ダッシュボード、振り返り | `c-card` + `c-section-header` + `c-action-group`。 | role別の表示分岐、リンク先。 |
| 4 | 役割別空状態 | ダッシュボード、選手、振り返り | `c-empty-state` + 画面固有の文言・主操作。 | コーチ／保護者の権限判定。 |

### 受入条件

- 数値に`--font-numeric`、`tabular-nums`、必要な単位・注釈が維持される。
- 320pxでランキング行、履歴、長い選手名、補足文が横にはみ出さない。
- 保護者・コーチ・未登録・データありの分岐で、同じ主操作と遷移先が表示される。
- `dashboard.css`と`base.css`にある対象パターンの重複規則を、移行後に削除候補として台帳更新できる。

## 7. 波2：フォーム・モーダル・データ管理を統合する

### 優先理由

`components.css`に残る旧フォーム・モーダルの互換規則は、テーマ変更や形状規約の再適用を難しくします。設定と練習で完成した`c-form-field`、`c-fieldset`、`c-modal`を、通常業務画面へ横展開します。

### 移行順序

| 順序 | 対象 | 置換先 | 変更しないもの | 受入条件 |
|---:|---|---|---|---|
| 1 | 選手追加・編集・CSV導線 | `c-modal` + `c-form-field` + `c-action-group` | CSV形式、保存キー、選手ID。 | バリデーション、取込、詳細遷移が同じ。 |
| 2 | メニュー・戦術の作成／編集 | `c-modal` + `c-form-field` + `c-fieldset` | カテゴリ、タグ、戦術プレビュー。 | 作成・編集・削除のイベント接続が同じ。 |
| 3 | 同期・バックアップ・復元・削除 | `c-card` + `c-section-header` + `c-action-group`。必要なら`c-confirm-dialog`。 | ファイル形式、同期プロトコル、復元ロジック。 | danger操作に確認文言・状態表示・キャンセルがある。 |
| 4 | 旧`.modal` / `.bottom-sheet-modal` | 標準`c-modal`の構造へ段階移行。 | フォーカス戻し、既存ID、閉じる処理。 | 本文スクロールとフッター操作が競合しない。 |

### 受入条件

- ラベル、説明、必須、エラー、入力、保存操作が一貫した文書フローになる。
- モーダルの本文とフッターが小型端末で重ならない。
- 破壊的操作はdangerロールと確認手順を使い、Liquid UIやガラス表現で危険度を曖昧にしない。
- フォーム関連の新規`!important`を追加しない。

## 8. 波3：試合詳細・招集・タイムラインを構造単位で移行する

### 優先理由

試合詳細は通常業務画面の中で最も高いデータ整合性リスクを持ちます。すでに基本情報・テーマ・総括フォームは移行済みであるため、残る招集、出場設定、タイムライン、スコア周辺を小さな単位へ分割します。

### 移行順序

| 順序 | 対象 | 置換先 | 固有のまま残すもの | 受入条件 |
|---:|---|---|---|---|
| 1 | 招集・出場設定 | `c-roster-row` + `c-fieldset` | 選手ID、招集状態、保存形式。 | 選択・状態変更・保存後の再表示が一致する。 |
| 2 | 試合タイムライン | `c-data-list`。二画面一致時のみ`c-timeline-item`。 | イベント順、イベントID、編集・削除処理。 | 得点、交代、警告、メモの順序が不変。 |
| 3 | スコア・PK編集の周辺操作 | `c-action-group`、数値トークン、状態ロール。 | 得点計算、PKロジック、結果判定。 | 集計結果が変更前と完全に一致する。 |
| 4 | 試合詳細の旧表示カード | `c-card` + `c-section-header`。 | 画面遷移、共有、エクスポート。 | 長い大会名・相手名・結果でも崩れない。 |

### 追加検証

- 試合データを作成・編集・削除して保存値を比較する。
- タイムラインの時刻順、同時刻イベント、編集後の並び順を確認する。
- モバイル幅で招集名簿の選手名・状態・操作が同時に確認できる。
- P35へ対象ID、イベント種別、標準部品クラスの契約を追加する。

## 9. 波4：Field Companionを安全優先で限定共通化する

### 優先理由

Field Companionは試合中の片手操作、屋外視認性、オフライン記録、連続入力を伴います。通常画面のカード規約をそのまま適用せず、意味トークン・アイコン・名簿構造・操作群だけを共有します。

### 移行順序

| 順序 | 対象 | 共通化する範囲 | 固有規約 | 受入条件 |
|---:|---|---|---|---|
| 1 | ライブスコア・時計 | 数値フォント、意味トークン、見出し構造。 | 大きな数値、視認距離、時計制御。 | ライト／ダークでスコア・時計が即読できる。 |
| 2 | 出場中・ベンチ | `c-roster-row`の情報構造。 | 出場時間、交代操作、連続入力。 | 選手名・状態・交代先を同時に確認できる。 |
| 3 | クイック操作 | `c-icon`、action/dangerロール、`c-action-group`の並び原則。 | 最小タップ面、利き手、操作優先度。 | 主要操作が44px以上で、誤タップしない。 |
| 4 | 試合ログ・保存状態 | `c-data-list`の構造、テキスト併記の状態ロール。 | 直近イベント優先、同期・保留制御。 | オフライン、保留、同期済みを色だけで示さない。 |

### 追加検証

小型幅、ライト／ダーク、利き手左右、屋外視認性、画面回転、連続記録、Wake Lock、オフライン保存、復帰同期を確認します。Field Companionの主操作には、Frosted GlassやLiquid UIを導入しません。

## 10. 波5：作図・アニメーションを外側から標準化する

### 優先理由

作図はキャンバス座標、ドラッグ、Undo/Redo、フレーム操作を持つドメイン専用UIです。`drawing.css`をゼロにすることは目標にせず、通常UIの重複だけを安全に取り除きます。

### 移行順序

| 順序 | 対象 | 置換先 | 触らないもの | 受入条件 |
|---:|---|---|---|---|
| 1 | ツールバーのテキスト操作 | `c-action-group`、`c-icon`、共通ボタン形状。 | ツール選択状態、描画モード。 | キーボード・タップ操作が維持される。 |
| 2 | ピッチ設定・グリッド・保存設定 | `c-form-field`、`c-choice-field`、`c-fieldset`。 | キャンバス座標、スナップ計算。 | 設定変更が従来の描画結果を返す。 |
| 3 | フィルムストリップ・フレーム操作 | `c-data-list`または専用の軽量行構造。 | フレーム順、再生・Undo/Redo。 | 並び、選択、削除、再生が一致する。 |
| 4 | 確認・エクスポート | `c-modal`、`c-action-group`。 | 出力形式、保存先、ファイル生成。 | エクスポート結果が変更前と一致する。 |

### 追加検証

キャンバス座標、ドラッグ、タッチ、Undo/Redo、フレーム再生、保存、読み込み、エクスポートを個別に確認します。画面外のCSS変更が描画座標へ影響しないよう、キャンバス周辺の寸法は専用契約として残します。

## 11. 波6：レガシーCSSの削除と運用定着

### 実施タスク

| タスク | 実施内容 | 受入条件 |
|---|---|---|
| 未使用規則の削除 | 台帳で使用ゼロが確認できた旧`.card`、`.form-group`、`.modal`、`u-ext-*`、画面固有補正を削除する。 | 削除前後でP33/P34/P35/RWDおよび対象機能テストが通る。 |
| インライン指定の削減 | 画面単位で置換済みの`style="..."`を削除し、理由付きで残す指定を明文化する。 | 動的座標・JS更新以外の反復指定が共通クラスへ移る。 |
| CSS責務の再配置 | 画面固有でなくなった規則を`components-standard.css`または`components-system.css`へ移す。 | `base.css`と`components.css`に新しい画面固有規則を増やさない。 |
| 設計文書の更新 | 部品API、適用先、禁止事項、完了状況を更新する。 | 新規開発者が画面固有CSSではなく部品を選べる。 |

## 12. 各移行の標準手順

1. 台帳から対象パターンを一つ選び、利用箇所と残すDOM契約を列挙する。
2. 320、390、768、1024、1440pxの基準表示を記録する。
3. 既存`c-*`で不足する責務を確認し、二画面以上の利用根拠がある場合だけ新部品を設計する。
4. HTMLテンプレートまたは動的HTMLを、ID・`data-*`・イベント接続を保持したまま共通クラスへ置換する。
5. 対象CSSの規則を移行先へ移すか、画面固有として残す理由を台帳に記録する。
6. P33、P34、P35、対象の機能スモーク、レスポンシブ検証、`git diff --check`を実行する。
7. 1画面または1パターン単位でコミットし、公開後に比較する。
8. 次の波へ進む前に、台帳の状態と旧規則の使用数を更新する。

## 13. 共通受入基準

| 分類 | 必須条件 |
|---|---|
| DOM・イベント | 必要な`id`、`data-*`、フォーム名、イベント委譲先、保存キーを維持する。 |
| データ整合性 | 保存、復元、同期、フィルター、並び順、イベント順、エクスポートが従来と同じ。 |
| レイアウト | 320、390、768、1024、1440pxで横溢れ、不可視操作、固定UIの重なりがない。 |
| テーマ | 任意種色、light、darkで本文・補助文・主操作・境界・フォーカスが読める。 |
| タイポグラフィ | 日本語の長文、英数字、数値列、得点、時刻、長い選手名が崩れない。 |
| アクセシビリティ | ラベル、説明、エラー、状態、単独アイコンの`aria-label`、キーボードフォーカスを維持する。 |
| 表現規約 | 文字を含む操作は角丸矩形、円形はアイコン専用。ガラス面はナビ・モーダル等の文脈レイヤーに限定する。 |
| 回帰 | P33、P34、P35、対象機能スモーク、レスポンシブ25件、`git diff --check`を通過する。 |

## 14. コミットと公開の規約

| 変更単位 | 含めるもの | 含めないもの | 例 |
|---|---|---|---|
| 部品導入 | トークン、`c-*`、P33契約、設計文書。 | 画面の保存ロジック変更。 | `feat: add metric list component` |
| 画面移行 | 一画面または一パターンのHTML／動的HTML、対象CSS削減、P35契約。 | 別画面の一括リファクタリング。 | `refactor: migrate player stats to data list` |
| 高リスク移行 | 対象画面、機能スモーク、追加の契約。 | データ形式・同期仕様の変更。 | `refactor: standardize match roster rows` |
| 削除 | 使用ゼロの旧CSS、台帳更新。 | 新機能追加。 | `refactor: remove unused legacy modal rules` |

公開版は、画面単位または横断パターン単位でバージョンを更新し、Service Workerのキャッシュ世代も同時に更新します。

## 15. 完了の定義

本ロードマップの完了は、全CSSを共通部品へ移すことではありません。以下を満たした時点で「テーマ変更に強い標準化が完了した」と判断します。

1. 新規画面・新規機能が、トークン、`l-*`、`c-*`だけで構成される。
2. 通常業務画面に残る旧`.card`、`.form-group`、`.modal`、`u-ext-*`は互換・ドメイン固有のものだけであり、残す理由が台帳に記録されている。
3. ダッシュボード、選手、メニュー、戦術、振り返り、設定、試合詳細、Field Companion、作図について、カード・フォーム・一覧・空状態・モーダル・操作群の適用方針が明確である。
4. 作図の描画エンジンと、通常UIのスタイルが分離されている。
5. テーマ更新時に、P33、P34、P35、レスポンシブ検証、対象画面の機能スモークを毎回通せる。

この順序で進めることで、機能停止や全面再実装を避けながら、残る画面固有CSSを共通部品へ安全に集約できます。
