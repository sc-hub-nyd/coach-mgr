# レスポンシブ静的監査所見

主要なレスポンシブ規則は`base.css`、`components.css`、`dashboard.css`に分散しており、既存の画面別ブレークポイントとP33で追加した共通レイアウト部品が併存している。

| 対象 | 静的な確認結果 | 複数幅での確認重点 |
|---|---|---|
| 設定 | `.settings-grid`は`minmax(340px, 1fr)`だが、480px以下で一列に切替わる。 | 481〜700pxの中間幅で、列幅と余白が横溢れしないか確認する。 |
| Field Companion | 操作帯は5列から620px以下で3列へ、名簿は2列から1列へ切り替わる。390px以下では状態行も縦方向になる。 | 320px・390pxで時計、期間選択、7操作ボタン、名簿、試合ログを確認する。 |
| 振り返り | 指標は4列から960px以下で2列、コンテンツは2列から1列へ切り替わる。 | 768pxと390pxで比較カード・意思決定カードの折返しを確認する。 |
| 既存資産 | 固定幅・`nowrap`・`!important`は旧実装に残る。意図した横スクロール領域もある。 | `scrollWidth`、可視領域、テキスト切詰めをDOM検証し、外側ページの横スクロールと区別する。 |

現時点の静的確認では、P33で追加した`l-grid`は小型画面で1列化し、`c-section-header`と`c-action-group`も折返しを持つ。固定幅・既存の抽出CSSは残るため、合否は実レンダリングで判定する。
